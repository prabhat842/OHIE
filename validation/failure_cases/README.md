# Failure Cases

This suite documents when OHIE should not be trusted.

The goal is not to fail the code. The goal is to expose trust boundaries:

- timestep sensitivity
- terrain discontinuity artifacts
- boundary imposition artifacts
- unrealistic parameter regimes

