# First Experiment

Run the smallest reproducible benchmark:

```bash
python3 ../benchmark_demo.py
```

If you want a slightly richer scenario:

```bash
python3 ../flat_terrain_intervention_mvp.py
```

This folder exists to make the first step obvious for new contributors and reviewers.
