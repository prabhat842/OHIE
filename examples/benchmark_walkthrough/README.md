# Benchmark Walkthrough

1. Run `python3 ../benchmark_demo.py`.
2. Read the benchmark output.
3. Compare routing and persistence behavior against the written limitations in `docs/scientific_position.md`.
4. If you need terrain-sensitive behavior, run the transfer and terrain-regime studies under `validation/`.

This walkthrough is intentionally short. The goal is reproducibility, not training material inflation.
